{
  "name": "availcal-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that schedules the AvailCal merge Container and serves the feed from R2.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.22.0",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260623.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6",
    "wrangler": "^4.104.0"
  }
}
